<Window x:Class="Crosspose.Doctor.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Crosspose Doctor" Height="450" Width="700"
        Background="{DynamicResource WindowBackground}"
        WindowStartupLocation="CenterScreen"
        Loaded="OnLoaded">
    <DockPanel Background="{DynamicResource WindowBackground}">
        <Menu DockPanel.Dock="Top" Background="{DynamicResource SidebarBackground}" Foreground="{DynamicResource TextPrimary}">
            <Menu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource SidebarBackground}" />
                </Style>
            </Menu.Resources>
            <MenuItem Header="_File">
                <MenuItem Header="_Quit" Click="OnQuitClick" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Click="OnAboutClick" />
            </MenuItem>
        </Menu>
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="16,16,16,4">
                <Button x:Name="FixAllButton"
                        Content="Fix All"
                        DockPanel.Dock="Right"
                        IsEnabled="False"
                        Width="90"
                        Padding="6,4"
                        VerticalAlignment="Center"
                        Click="OnFixAllClick"
                        Background="{DynamicResource ButtonBackground}"
                        Foreground="{DynamicResource ButtonForeground}"
                        BorderBrush="{DynamicResource ButtonBorder}" />
                <TextBlock Text="Crosspose Doctor" Foreground="{DynamicResource TextPrimary}" FontSize="20" VerticalAlignment="Center" />
            </DockPanel>

            <Border x:Name="OfflineBanner"
                    Grid.Row="1"
                    Visibility="Collapsed"
                    Background="{DynamicResource SidebarBackground}"
                    BorderBrush="{DynamicResource BorderBrushColor}"
                    BorderThickness="0,1,0,1"
                    Padding="16,6">
                <TextBlock Foreground="{DynamicResource TextMuted}"
                           FontSize="12">
                    <Run FontWeight="SemiBold" Foreground="{DynamicResource StatusWarningFill}">Offline mode active.</Run>
                    <Run>Connectivity checks (Azure CLI, ACR auth) are suppressed.</Run>
                </TextBlock>
            </Border>

            <ListView x:Name="ChecksList"
                      Grid.Row="2"
                      Margin="12,4,12,12"
                      Background="{DynamicResource ListBackground}"
                      Foreground="{DynamicResource TextPrimary}"
                      BorderBrush="{DynamicResource ListBorder}"
                      BorderThickness="1"
                      MouseDoubleClick="OnChecksListDoubleClick">
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="2" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn Header="Fix" Width="70">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Fix"
                                            Padding="6,2"
                                            IsEnabled="{Binding IsFixEnabled}"
                                            Click="OnFixClick"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Check" Width="150" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Description" Width="260" DisplayMemberBinding="{Binding Description}" />
                        <GridViewColumn Header="Result" Width="200" DisplayMemberBinding="{Binding Result}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
