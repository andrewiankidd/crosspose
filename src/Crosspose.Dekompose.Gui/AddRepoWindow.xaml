<Window x:Class="Crosspose.Dekompose.Gui.AddChartSourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Chart Source"
        Height="210"
        Width="520"
        MinWidth="400"
        MinHeight="210"
        MaxHeight="210"
        Background="{DynamicResource WindowBackground}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Address" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" />
            <TextBox Grid.Column="1"
                     x:Name="AddressBox"
                     ToolTip="Required"
                     Background="{DynamicResource InputBackground}"
                     Foreground="{DynamicResource TextPrimary}"
                     BorderBrush="{DynamicResource InputBorder}"
                     VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Username" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" />
            <TextBox Grid.Column="1"
                     x:Name="UserBox"
                     ToolTip="Leave blank if unauthenticated or using SSO"
                     Background="{DynamicResource InputBackground}"
                     Foreground="{DynamicResource TextPrimary}"
                     BorderBrush="{DynamicResource InputBorder}"
                     VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Password" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" />
            <PasswordBox Grid.Column="1"
                         x:Name="PassBox"
                         ToolTip="Leave blank if unauthenticated or using SSO"
                         Background="{DynamicResource InputBackground}"
                         Foreground="{DynamicResource TextPrimary}"
                         BorderBrush="{DynamicResource InputBorder}"
                         VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Filter" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" />
            <TextBox Grid.Column="1"
                     x:Name="FilterBox"
                     ToolTip="Optional client side result filter"
                     Background="{DynamicResource InputBackground}"
                     Foreground="{DynamicResource TextPrimary}"
                     BorderBrush="{DynamicResource InputBorder}"
                     VerticalAlignment="Center" />
        </Grid>

        <Button Grid.Row="4"
                x:Name="AddButton"
                Content="+ ADD"
                Margin="0,0,0,8"
                Height="32"
                Background="{DynamicResource AccentPositive}"
                Foreground="White"
                BorderBrush="{DynamicResource ButtonBorder}"
                Click="OnAddClick" />

        <TextBlock Grid.Row="5"
                   x:Name="StatusText"
                   Foreground="{DynamicResource TextMuted}"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
