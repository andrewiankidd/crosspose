<Window x:Class="Crosspose.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
        xmlns:local="clr-namespace:Crosspose.Gui"
        Title="Crosspose"
        Height="600"
        Width="960"
        MinWidth="720"
        MinHeight="500"
        Background="{DynamicResource WindowBackground}"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Loaded="OnLoaded"
        Closing="OnMainWindowClosing">
    <Window.Resources>
        <CollectionViewSource x:Key="ContainersView" Source="{Binding AllContainers}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="ProjectDisplay" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top" Background="{DynamicResource SidebarBackground}" Foreground="{DynamicResource TextPrimary}">
            <MenuItem Header="_File">
                <MenuItem Header="_Quit" Click="OnQuitClick" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Log View" Click="OnLogViewClick" />
                <Separator />
                <MenuItem Header="{Binding ThemeMenuHeader}" Click="OnToggleThemeClick" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="Crosspose Tools">
                    <MenuItem Header="Crosspose _Doctor" Click="OnDoctorClick" />
                    <MenuItem Header="Crosspose _Dekompose" Click="OnDekomposeClick" />
                </MenuItem>
                <MenuItem Header="Container Tools">
                    <MenuItem Header="Docker _Desktop" Click="OnDockerDesktopClick" />
                    <MenuItem Header="_Podman Desktop" Click="OnPodmanDesktopClick" />
                </MenuItem>
                <Separator />
                <MenuItem Header="Open _Configuration Directory" Click="OnOpenConfigDirectoryClick" />
                <Separator />
                <MenuItem Header="{Binding OfflineModeMenuHeader}"
                          Click="OnToggleOfflineModeClick" />
                <MenuItem Header="Enable _Portable Mode"
                          Click="OnEnablePortableModeClick"
                          Visibility="{Binding PortableModeMenuVisibility}" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About" Click="OnAboutClick" />
            </MenuItem>
        </Menu>

        <Grid xmlns:ui="clr-namespace:Crosspose.Ui;assembly=Crosspose.Ui">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Background="{DynamicResource SidebarBackground}" BorderBrush="{DynamicResource BorderBrushColor}" BorderThickness="0,0,1,0">
                <DockPanel>
                    <!-- Doctor status badge — always visible at the bottom of the sidebar -->
                    <Border DockPanel.Dock="Bottom"
                            BorderBrush="{DynamicResource BorderBrushColor}"
                            BorderThickness="0,1,0,0"
                            Padding="8,6">
                        <Button Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="OnDoctorClick"
                                HorizontalContentAlignment="Left"
                                Padding="0"
                                ToolTip="{Binding DoctorStatusTooltip}">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,7,0">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="{DynamicResource StatusNeutralFill}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DoctorIsReady}" Value="True">
                                                    <Setter Property="Fill" Value="{DynamicResource StatusReadyFill}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DoctorIsNeedsAttention}" Value="True">
                                                    <Setter Property="Fill" Value="{DynamicResource StatusWarningFill}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding DoctorStatusLabel}"
                                           FontSize="11"
                                           Foreground="{DynamicResource TextMuted}"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </Border>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="6">
                            <TextBlock Text="Setup" Margin="6,6,6,4" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" />
                            <ListBox x:Name="SidebarSetup"
                                     SelectionChanged="OnSidebarSelectionChanged"
                                     Background="{DynamicResource SidebarBackground}"
                                     BorderBrush="{DynamicResource BorderBrushColor}"
                                     Foreground="{DynamicResource TextPrimary}">
                                <ListBoxItem x:Name="SidebarChartsItem" Content="Helm Charts" Tag="Charts" />
                                <ListBoxItem x:Name="SidebarBundlesItem" Content="Compose Bundles" Tag="Compose Bundles" />
                            </ListBox>

                            <TextBlock Text="Runtime" Margin="6,12,6,4" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" />
                            <ListBox x:Name="SidebarRuntime"
                                     SelectionChanged="OnSidebarSelectionChanged"
                                     Background="{DynamicResource SidebarBackground}"
                                     BorderBrush="{DynamicResource BorderBrushColor}"
                                     Foreground="{DynamicResource TextPrimary}">
                                <ListBoxItem x:Name="SidebarProjectsItem" Content="Projects" Tag="Projects" />
                                <ListBoxItem x:Name="SidebarContainersItem" Content="Containers" Tag="Containers" />
                                <ListBoxItem x:Name="SidebarImagesItem" Content="Images" Tag="Images" />
                                <ListBoxItem x:Name="SidebarVolumesItem" Content="Volumes" Tag="Volumes" />
                            </ListBox>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="12">
                    <TextBlock x:Name="ViewTitle"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimary}" />
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap">
                        <Run Text="{Binding InfoText}" />
                        <Run Text=" " />
                        <Hyperlink NavigateUri="http://todo/"
                                   RequestNavigate="OnLearnMore"
                                   Foreground="{DynamicResource TextPrimary}">
                            <Run Text="Learn More" />
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>

                <Border Grid.Row="1" Margin="12" Background="{DynamicResource PanelBackground}" BorderBrush="{DynamicResource BorderBrushColor}" BorderThickness="1">
                    <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,4,0,8">
                    <!-- Charts: New/Dekompose accent colors controlled dynamically in UpdateChartButtons() -->
                    <StackPanel x:Name="ChartsToolbar" Orientation="Horizontal" Visibility="Collapsed">
                        <Button x:Name="ChartsOpenFolderButton" Padding="10,4" Click="OnChartsOpenFolder" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="FolderOpen" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Open Folder" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ChartsNewButton" Padding="10,4" Click="OnChartsNew" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Add" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="New" />
                            </StackPanel>
                        </Button>
                        <Separator Margin="4,2" />
                        <Button x:Name="ChartsSetValuesButton" Padding="10,4" Click="OnChartsSetValuesFile" Margin="4,0,8,0" IsEnabled="False"
                                ToolTip="Associate a values.yaml with this chart. Stored alongside the chart and passed to Dekompose automatically.">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="DocumentSettings" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Values" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ChartsSetDekomposeConfigButton" Padding="10,4" Click="OnChartsSetDekomposeConfig" Margin="0,0,8,0" IsEnabled="False"
                                ToolTip="Associate a dekompose.yml with this chart. Stored alongside the chart and used as conversion rules when Dekomposing.">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="DocumentEdit" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Config" />
                            </StackPanel>
                        </Button>
                        <Separator Margin="4,2" />
                        <Button x:Name="ChartsDekomposeButton" Padding="10,4" Click="OnChartsDekompose" Margin="4,0,8,0" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Play" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Dekompose" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ChartsDeleteButton" Tag="AccentDestructive" Padding="10,4" Click="OnChartsDelete" Margin="0,0,8,0" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Delete" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ChartsRefreshButton" Padding="8,4" Click="OnRefreshClick">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ArrowClockwise" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Refresh" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <!-- Compose Bundles: New redirects to Helm Charts (bundles are created via Dekompose from there) -->
                    <StackPanel x:Name="DefinitionsToolbar" Orientation="Horizontal" Visibility="Collapsed">
                        <Button x:Name="ProjectsNewButton" Tag="AccentPositive" Padding="10,4" Click="OnProjectsNew" Margin="0,0,8,0"
                                ToolTip="Go to Helm Charts to pull and Dekompose a chart into a new bundle">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Add" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="New" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ProjectsOpenButton" Padding="10,4" Click="OnProjectsOpen" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="FolderOpen" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Open Folder" />
                            </StackPanel>
                        </Button>
                        <Separator Margin="4,2" />
                        <Button x:Name="ProjectsDeployButton" Tag="AccentPositive" Padding="10,4" Click="OnProjectsDeploy" Margin="4,0,8,0" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Rocket" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Deploy" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ProjectsDeleteProjectButton" Tag="AccentDestructive" Padding="10,4" Click="OnProjectsDeleteProject" Margin="0,0,8,0" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Delete" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DefinitionsRefreshButton" Padding="8,4" Click="OnRefreshClick">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ArrowClockwise" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Refresh" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel x:Name="DeploymentsToolbar" Orientation="Horizontal" Visibility="Collapsed">
                        <Button x:Name="DeploymentsOpenButton" Padding="10,4" Click="OnDeploymentsOpenFolder" Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="FolderOpen" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Open Folder" />
                            </StackPanel>
                        </Button>
                        <Separator Margin="4,2" />
                        <Button x:Name="DeploymentsUpButton" Tag="AccentPositive" Padding="10,4" Margin="4,0,8,0" Click="OnDeploymentsUp" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ChevronUp" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Up" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeploymentsStartButton" Tag="AccentPositive" Padding="10,4" Margin="0,0,8,0" Click="OnDeploymentsStart" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Play" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Start" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeploymentsStopButton" Padding="10,4" Margin="0,0,8,0" Click="OnDeploymentsStop" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Stop" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Stop" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeploymentsRestartButton" Padding="10,4" Margin="0,0,8,0" Click="OnDeploymentsRestart" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ArrowSync" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Restart" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeploymentsDownButton" Padding="10,4" Margin="0,0,8,0" Click="OnDeploymentsDown" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ChevronDown" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Down" />
                            </StackPanel>
                        </Button>
                        <Separator Margin="4,2" />
                        <Button x:Name="DeploymentsRemoveButton" Tag="AccentDestructive" Padding="10,4" Margin="4,0,8,0" Click="OnDeploymentsRemove" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Delete" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeploymentsRefreshButton" Padding="8,4" Click="OnRefreshClick">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ArrowClockwise" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Refresh" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel x:Name="ContainersToolbar" Orientation="Horizontal" Visibility="Collapsed">
                        <Button x:Name="ContainersStartButton" Tag="AccentPositive" Padding="10,4" Margin="0,0,8,0" Click="OnContainersStart" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Play" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Start" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ContainersStopButton" Padding="10,4" Margin="0,0,8,0" Click="OnContainersStop" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Stop" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Stop" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ContainersDetailsButton" Padding="10,4" Margin="0,0,8,0" Click="OnContainersDetails" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Info" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Details" />
                            </StackPanel>
                        </Button>
                        <Separator Margin="4,2" />
                        <Button x:Name="ContainersDeleteButton" Tag="AccentDestructive" Padding="10,4" Margin="4,0,8,0" Click="OnContainersDelete" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Delete" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ContainersRefreshButton" Padding="8,4" Click="OnRefreshClick">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ArrowClockwise" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Refresh" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel x:Name="ImagesToolbar" Orientation="Horizontal" Visibility="Collapsed">
                        <Button x:Name="ImagesDeleteButton" Tag="AccentDestructive" Padding="10,4" Margin="0,0,8,0" Click="OnImagesDelete" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Delete" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ImagesPruneButton" Tag="AccentDestructive" Padding="10,4" Margin="0,0,8,0" Click="OnImagesPull">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="LeafTwo" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Prune Unused" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="ImagesRefreshButton" Padding="8,4" Click="OnRefreshClick">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ArrowClockwise" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Refresh" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                    <StackPanel x:Name="VolumesToolbar" Orientation="Horizontal" Visibility="Collapsed">
                        <Button x:Name="VolumesInspectButton" Padding="10,4" Margin="0,0,8,0" Click="OnVolumesInspect" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Search" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Inspect" />
                            </StackPanel>
                        </Button>
                        <Separator Margin="4,2" />
                        <Button x:Name="VolumesDeleteButton" Tag="AccentDestructive" Padding="10,4" Margin="4,0,8,0" Click="OnVolumesDelete" IsEnabled="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="Delete" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="VolumesPruneButton" Tag="AccentDestructive" Padding="10,4" Margin="0,0,8,0" Click="OnVolumesPrune">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="LeafTwo" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Prune Unused" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="VolumesRefreshButton" Padding="8,4" Click="OnRefreshClick">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <ic:FluentIcon Icon="ArrowClockwise" IconVariant="Filled" IconSize="Size16" Width="16" Height="16" Margin="0,0,6,0" />
                                <TextBlock Text="Refresh" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <ListView Grid.Row="2"
                          x:Name="ChartsList"
                          Visibility="Collapsed"
                          ItemsSource="{Binding Charts}"
                          SelectionChanged="OnChartsSelectionChanged"
                          SelectionMode="Single"
                          Background="{DynamicResource ListBackground}"
                          Foreground="{DynamicResource TextPrimary}"
                          BorderBrush="{DynamicResource ListBorder}">
                    <ListView.Resources>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.Resources>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Filename" DisplayMemberBinding="{Binding FileName}" Width="280" />
                            <GridViewColumn Header="Size" DisplayMemberBinding="{Binding SizeDisplay}" Width="90" />
                            <GridViewColumn Header="Modified" DisplayMemberBinding="{Binding Modified}" Width="150" />
                            <GridViewColumn Header="Files" DisplayMemberBinding="{Binding AssociatedFilesLabel}" Width="120" />
                            <GridViewColumn Header="Path" DisplayMemberBinding="{Binding FullPath}" Width="280" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <Grid Grid.Row="2"
                      x:Name="ProjectsPanel"
                      Visibility="Collapsed">
                    <ListView x:Name="ProjectsListView"
                              ItemsSource="{Binding Projects}"
                              SelectionChanged="OnProjectsSelectionChanged"
                              Background="{DynamicResource ListBackground}"
                              Foreground="{DynamicResource TextPrimary}"
                              BorderBrush="{DynamicResource ListBorder}">
                        <ListView.Resources>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.Resources>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="220" />
                                <GridViewColumn Header="Version" DisplayMemberBinding="{Binding Version}" Width="140" />
                                <GridViewColumn Header="Last Modified" DisplayMemberBinding="{Binding LastModified}" Width="180" />
                                <GridViewColumn Header="Path" DisplayMemberBinding="{Binding FullPath}" Width="320" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>

                        <ListView Grid.Row="2"
                                  x:Name="DeploymentsList"
                                  Visibility="Collapsed"
                                  ItemsSource="{Binding Deployments}"
                                  SelectionMode="Single"
                                  SelectionChanged="OnDeploymentsSelectionChanged"
                                  Background="{DynamicResource ListBackground}"
                                  Foreground="{DynamicResource TextPrimary}"
                                  BorderBrush="{DynamicResource ListBorder}">
                            <ListView.Resources>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Project" Width="200" DisplayMemberBinding="{Binding Project}" />
                                    <GridViewColumn Header="Version" Width="120" DisplayMemberBinding="{Binding Version}" />
                                    <GridViewColumn Header="Status" Width="140" DisplayMemberBinding="{Binding PlatformStatus}" />
                                    <GridViewColumn Header="Last Action" Width="140" DisplayMemberBinding="{Binding LastAction}" />
                                    <GridViewColumn Header="Updated" Width="140" DisplayMemberBinding="{Binding LastUpdated}" />
                                    <GridViewColumn Header="Path" Width="240" DisplayMemberBinding="{Binding FullPath}" />
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <ListView Grid.Row="2"
                                  x:Name="ContainersList"
                                  Visibility="Visible"
                                  Background="{DynamicResource PanelBackground}"
                                  Foreground="{DynamicResource TextPrimary}"
                                  BorderThickness="0"
                                  MouseDoubleClick="OnContainerDoubleClick"
                                  SelectionChanged="OnContainersSelectionChanged">
                            <ListView.Resources>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.ItemsSource>
                                <Binding Source="{StaticResource ContainersView}" />
                            </ListView.ItemsSource>
                            <ListView.GroupStyle>
                                <GroupStyle ContainerStyle="{StaticResource GroupedListGroupContainerStyle}" />
                            </ListView.GroupStyle>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Width="32">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                          HorizontalAlignment="Center"
                                                          Checked="OnContainerSelectionChanged"
                                                          Unchecked="OnContainerSelectionChanged" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Width="24">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Width="16" Height="16"
                                                       Source="{Binding HostPlatform, Converter={StaticResource PlatformIconConverter}}" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Status" Width="110">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Ellipse Width="12" Height="12" Margin="0,0,6,0">
                                                        <Ellipse.Style>
                                                            <Style TargetType="Ellipse">
                                                                <Setter Property="Fill" Value="{Binding EffectiveState, Converter={StaticResource StatusBrushConverter}}" />
                                                                <Setter Property="Stroke" Value="Transparent" />
                                                                <Setter Property="StrokeThickness" Value="0" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ExitState}" Value="{x:Static local:ContainerExitState.ExitedSuccess}">
                                                                        <Setter Property="Fill" Value="Transparent" />
                                                                        <Setter Property="Stroke" Value="{StaticResource SuccessStrokeBrush}" />
                                                                        <Setter Property="StrokeThickness" Value="2" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Ellipse.Style>
                                                    </Ellipse>
                                                    <TextBlock Text="{Binding DisplayState}" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Container" Width="200" DisplayMemberBinding="{Binding Id}" />
                                    <GridViewColumn Header="Image" Width="200" DisplayMemberBinding="{Binding Image}" />
                                    <GridViewColumn Header="Ports" Width="160">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Ports}"
                                                           MouseLeftButtonUp="OnContainerPortsClick">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                                            <Setter Property="Cursor" Value="Hand" />
                                                            <Setter Property="TextDecorations" Value="Underline" />
                                                            <Style.Triggers>
                                                                <Trigger Property="Text" Value="">
                                                                    <Setter Property="Cursor" Value="Arrow" />
                                                                    <Setter Property="TextDecorations" Value="{x:Null}" />
                                                                    <Setter Property="Opacity" Value="0.7" />
                                                                </Trigger>
                                                                <Trigger Property="Text" Value="{x:Null}">
                                                                    <Setter Property="Cursor" Value="Arrow" />
                                                                    <Setter Property="TextDecorations" Value="{x:Null}" />
                                                                    <Setter Property="Opacity" Value="0.7" />
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <TextBlock x:Name="PlaceholderText"
                                   Grid.Row="2"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource TextMuted}"
                                   FontSize="16"
                                   Visibility="Collapsed"
                                   Text="Not implemented yet" />

                        <ListView Grid.Row="2"
                                  x:Name="ImagesList"
                                  Visibility="Collapsed"
                                  ItemsSource="{Binding Images}"
                                  SelectionChanged="OnImagesSelectionChanged"
                                  SelectionMode="Extended"
                                  Background="{DynamicResource ListBackground}"
                                  Foreground="{DynamicResource TextPrimary}"
                                  BorderBrush="{DynamicResource ListBorder}">
                            <ListView.Resources>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                        <Setter Property="BorderBrush" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Setter Property="Padding" Value="2" />
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="VerticalContentAlignment" Value="Center" />
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                            </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Width="40">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                  Focusable="False"
                                                                  HorizontalAlignment="Center"
                                                                  Checked="OnImageSelectionChanged"
                                                                  Unchecked="OnImageSelectionChanged" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="" Width="32">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Width="16"
                                                       Height="16"
                                                       HorizontalAlignment="Center"
                                                       Source="{Binding HostPlatform, Converter={StaticResource PlatformIconConverter}}" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Status" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Ellipse Width="12" Height="12" Fill="{Binding State, Converter={StaticResource StatusBrushConverter}}" Margin="0,0,6,0" />
                                                    <TextBlock Text="{Binding State}" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Name" Width="180" DisplayMemberBinding="{Binding Name}" />
                                    <GridViewColumn Header="Tag" Width="120" DisplayMemberBinding="{Binding Tag}" />
                                    <GridViewColumn Header="Image ID" Width="200" DisplayMemberBinding="{Binding Id}" />
                                    <GridViewColumn Header="Size" Width="120" DisplayMemberBinding="{Binding Size}" />
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <ListView Grid.Row="2"
                                  x:Name="VolumesList"
                                  Visibility="Collapsed"
                                  ItemsSource="{Binding Volumes}"
                                  SelectionChanged="OnVolumesSelectionChanged"
                                  SelectionMode="Extended"
                                  Background="{DynamicResource ListBackground}"
                                  Foreground="{DynamicResource TextPrimary}"
                                  BorderBrush="{DynamicResource ListBorder}">
                            <ListView.Resources>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                        <Setter Property="BorderBrush" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Setter Property="Padding" Value="2" />
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="VerticalContentAlignment" Value="Center" />
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                            </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SidebarBackground}" />
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Width="40">
                                                <GridViewColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                  Focusable="False"
                                                                  HorizontalAlignment="Center"
                                                                  Checked="OnVolumeSelectionChanged"
                                                                  Unchecked="OnVolumeSelectionChanged" />
                                                    </DataTemplate>
                                                </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="" Width="32">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Width="16"
                                                       Height="16"
                                                       HorizontalAlignment="Center"
                                                       Source="{Binding HostPlatform, Converter={StaticResource PlatformIconConverter}}" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Status" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Ellipse Width="12" Height="12" Fill="{Binding State, Converter={StaticResource StatusBrushConverter}}" Margin="0,0,6,0" />
                                                    <TextBlock Text="{Binding State}" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Name" Width="220" DisplayMemberBinding="{Binding Name}" />
                                    <GridViewColumn Header="Size" Width="140" DisplayMemberBinding="{Binding Size}" />
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <ui:LoadingOverlay x:Name="ViewLoadingOverlay" Grid.Row="2" Visibility="Collapsed" />
                    </Grid>
                </Border>

                <ui:LogViewerControl Grid.Row="2"
                                     Margin="12,0,12,12"
                                     LogText="{Binding LogOutput, Mode=OneWay}"
                                     ClearRequested="OnLogClearRequested" />
            </Grid>
        </Grid>
    </DockPanel>
</Window>
